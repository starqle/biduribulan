%h1 Listing users

%table
  %tr
    %th Email
    %th Role
    %th Password
    %th
    %th
    %th

  - @users.each do |user|
    %tr
      %td= user.email
      %td= user.roles.map{|x| x.name}.join(', ')
      %td= user.password
      %td= link_to 'Show', [:admin, user]
      %td= link_to 'Edit', edit_admin_user_path(user)
      %td= link_to 'Destroy', [:admin, user], :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New User', new_admin_user_path
